# 배포하기

이 폴더는 **이미 git 저장소로 준비돼 있습니다.** 커밋도 끝나 있고,
GitHub 로그인(`inu-sudo`)도 완료된 상태입니다.

## 제일 빠른 방법 — 스크립트 한 번 실행

PowerShell을 열고 아래 한 줄을 붙여넣으세요. 저장소 생성 → 업로드 →
사이트 켜기까지 전부 자동입니다.

```powershell
powershell -ExecutionPolicy Bypass -File "$([Environment]::GetFolderPath('Desktop'))\claude\farmkogls-site\deploy.ps1"
```

스크립트는 올리기 **직전에 엑셀 파일이 섞여 있는지 검사**하고,
하나라도 있으면 업로드를 중단합니다.

아래는 위 스크립트가 대신 해주는 일을 하나씩 손으로 하는 방법입니다.

---

## 0. 이 폴더로 이동

```powershell
cd "$([Environment]::GetFolderPath('Desktop'))\claude\farmkogls-site"
```

## 1. GitHub 로그인 (한 번만)

```powershell
gh auth login --hostname github.com --git-protocol https --web
```

- `Login with a web browser` 선택
- 화면에 나오는 **8자리 코드**를 복사
- Enter → 브라우저가 열리면 코드를 붙여넣고 승인

> GitHub 계정이 없으면 <https://github.com/signup> 에서 먼저 만드세요.

## 2. 저장소 만들고 올리기

```powershell
gh repo create farmkogls-console --public --source=. --remote=origin --push
```

비공개로 두고 싶으면 `--public` 을 `--private` 으로 바꾸세요.
(단, 비공개면 GitHub Pages 무료 플랜으로는 배포가 안 됩니다 — 아래 참고)

## 3. 사이트 켜기

```powershell
gh api -X POST "repos/:owner/farmkogls-console/pages" -f "source[branch]=main" -f "source[path]=/"
```

이 명령이 안 되면 웹에서: **저장소 → Settings → Pages →
Source `Deploy from a branch` → Branch `main` / `(root)` → Save**

1~2분 뒤 아래 주소가 열립니다.

```
https://<GitHub사용자명>.github.io/farmkogls-console/
```

주소 확인:

```powershell
gh api "repos/:owner/farmkogls-console/pages" --jq .html_url
```

---

## 앞으로 내용을 고칠 때

```powershell
cd "$([Environment]::GetFolderPath('Desktop'))\claude\farmkogls-console"
python build.py
python build_site.py
python build_docs.py
Copy-Item site\index.html ..\farmkogls-site\index.html -Force
Copy-Item site\docs.html  ..\farmkogls-site\docs.html  -Force

cd ..\farmkogls-site
git add -A
git commit -m "내용 수정"
git push
```

push하면 1~2분 뒤 사이트에 자동 반영됩니다.

---

## PIN 바꾸기

PIN은 **`farmkogls-console\pin.txt` 파일 안에만** 있습니다.
이 파일은 **저장소에 올라가지 않습니다**(`.gitignore`로 차단).

```powershell
Set-Content "$([Environment]::GetFolderPath('Desktop'))\claude\farmkogls-console\pin.txt" "1234" -NoNewline
```

숫자를 바꾼 뒤 위의 "내용을 고칠 때" 과정을 그대로 하면 새 PIN이 적용됩니다.

> **왜 파일로 뺐나** — 처음에는 빌드 스크립트 안에 `PIN = "...."` 로 적혀 있었는데,
> 그 스크립트가 저장소에 같이 올라가면서 **PIN이 공개돼 버렸습니다.**
> 문에 비밀번호를 적어 붙여놓은 셈이었습니다. 이제 빌드할 때마다
> **결과물에 PIN 숫자가 들어 있으면 빌드를 거부**합니다.

---

## 비공개 저장소로 두고 싶다면

GitHub Pages 무료 플랜은 공개 저장소에서만 됩니다.
소스는 비공개로 두고 사이트만 공개하려면 아래 중 하나를 쓰세요 (모두 무료).

| 서비스 | 방법 |
|---|---|
| **Cloudflare Pages** | dash.cloudflare.com → Workers & Pages → Create → Pages → Connect to Git → 이 저장소 선택 → 빌드 명령 **비움**, 출력 디렉터리 **`/`** |
| **Netlify** | app.netlify.com → Add new site → Import an existing project → Publish directory **`/`** |
| **Vercel** | vercel.com/new → 저장소 선택 → Framework **Other**, Output **`/`** |

셋 다 push하면 자동 재배포됩니다.

---

## PIN보다 강한 잠금이 필요하면

**Cloudflare Access** 를 사이트 앞에 걸면 이메일 인증이 붙습니다.
무료 플랜으로 **50명**까지 됩니다. 허용할 이메일 주소만 등록해 두면
그 사람들만 들어올 수 있고, 그 경우 PIN은 없애도 됩니다.

Cloudflare Pages로 배포한 다음 → Zero Trust → Access → Applications →
Add an application → Self-hosted → 도메인 지정 → 정책에 이메일 목록 추가.
